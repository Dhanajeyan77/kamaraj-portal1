name: "sandbox-cage"
mode: ONCE
hostname: "Kamaraj-Sandbox"
cwd: "/app"
time_limit: 20
rlimit_as: 8192    # High enough for Java 21; safe for Python too

# Scratch space for compilers to write temporary class/object files
mount { 
  dst: "/tmp"
  fstype: "tmpfs"
  rw: true 
}