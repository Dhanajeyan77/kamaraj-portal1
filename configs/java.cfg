name: "sandbox-cage"
mode: ONCE
hostname: "Kamaraj-Sandbox"
cwd: "/tmp"              # Change from /app to /tmp
time_limit: 20
rlimit_as: 8192         # Increase from 5120 to 6144 (6GB)
rlimit_fsize: 100        # Allow small file creation

mount { 
  dst: "/tmp"
  fstype: "tmpfs"
  rw: true 
}